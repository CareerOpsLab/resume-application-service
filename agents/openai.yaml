interface:
  display_name: "简历定向优化与代投递"
  short_description: "定向改简历、筛岗位并安全协助投递"
  default_prompt: "使用 $resume-application-service 处理这位顾客：先分析资料和 JD，再完成定向简历、岗位筛选、逐岗确认的半自动投递和投递总表。"

policy:
  allow_implicit_invocation: true
